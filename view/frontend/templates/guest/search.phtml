<?php
declare(strict_types=1);

/** @var \Magento\Framework\View\Element\Template $block */
/** @var \Magento\Framework\Escaper $escaper */
?>
<div class="max-w-md mx-auto my-12" style="font-family: 'Inter', system-ui, sans-serif;">
    <div class="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden transition-all duration-300 hover:shadow-2xl">
        <!-- Header banner -->
        <div class="bg-gradient-to-r from-slate-900 to-slate-800 px-8 py-8 text-white text-center relative">
            <h2 class="text-2xl font-bold tracking-tight mb-2">
                <?= $escaper->escapeHtml(__('Order Return (RMA)')) ?>
            </h2>
            <p class="text-sm text-slate-300 font-light">
                <?= $escaper->escapeHtml(__('Look up your order to request a return or check status')) ?>
            </p>
        </div>

        <form action="<?= $escaper->escapeUrl($block->getUrl('rma/guest/search')) ?>" method="post" class="p-8 space-y-6">
            <?= $block->getBlockHtml('formkey') ?>

            <!-- Order Increment ID -->
            <div>
                <label for="order_id" class="block text-xs font-semibold uppercase tracking-wider text-slate-500 mb-2">
                    <?= $escaper->escapeHtml(__('Order ID / Number')) ?> <span class="text-red-500">*</span>
                </label>
                <div class="relative">
                    <input type="text"
                           name="order_id"
                           id="order_id"
                           required
                           placeholder="e.g. 000000001"
                           class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl text-slate-900 font-medium placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-slate-900 focus:border-transparent focus:bg-white transition-all"/>
                </div>
            </div>

            <!-- Email Address -->
            <div>
                <label for="email" class="block text-xs font-semibold uppercase tracking-wider text-slate-500 mb-2">
                    <?= $escaper->escapeHtml(__('Email Address')) ?> <span class="text-red-500">*</span>
                </label>
                <input type="email"
                       name="email"
                       id="email"
                       required
                       placeholder="your@email.com"
                       class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl text-slate-900 font-medium placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-slate-900 focus:border-transparent focus:bg-white transition-all"/>
            </div>

            <!-- Billing Last Name -->
            <div>
                <label for="lastname" class="block text-xs font-semibold uppercase tracking-wider text-slate-500 mb-2">
                    <?= $escaper->escapeHtml(__('Billing Last Name')) ?> <span class="text-red-500">*</span>
                </label>
                <input type="text"
                       name="lastname"
                       id="lastname"
                       required
                       placeholder="<?= $escaper->escapeHtml(__('e.g. Kowalski')) ?>"
                       class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-xl text-slate-900 font-medium placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-slate-900 focus:border-transparent focus:bg-white transition-all"/>
            </div>

            <!-- Submit Button -->
            <button type="submit"
                    class="w-full bg-slate-950 text-white font-semibold py-3.5 px-6 rounded-xl hover:bg-slate-800 active:scale-[0.98] transition-all shadow-md hover:shadow-lg flex items-center justify-center gap-2">
                <span>🔍</span>
                <span><?= $escaper->escapeHtml(__('Find Order')) ?></span>
            </button>
        </form>
    </div>
</div>
